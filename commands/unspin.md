---
description: Expose euphemism, evasion, and spin in a text (detector mode).
argument-hint: "[file path, URL, or pasted text]"
---

Invoke the `orwell-prose` skill in **detector mode** and analyze the target below.

Target: $ARGUMENTS

If `$ARGUMENTS` is a URL, fetch it; if a file path, read it; otherwise treat it as the text.
If no target is given, ask what to analyze.

Produce: a `quote → category → plain translation → what it conceals` table using the 9-category
evasion taxonomy; a **central evasion** paragraph (the gap between declared and real aims); and a
**fairness note** separating earnest abstraction from bad-faith concealment. Run the §9
internal-contradiction check but do not force it. **Translate to expose — never rewrite the text
to read "better,"** which would only launder it.
